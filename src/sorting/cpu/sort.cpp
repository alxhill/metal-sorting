#include "sort.h"

template <typename T>
std::vector<T> sort_bitonic(std::vector<T> values) {

}

template <typename T>
std::vector<T> sort_merge(std::vector<T> values) {

}

template <typename T>
std::vector<T> sort_stdlib(std::vector<T> values) {
    return values.sort();
}
